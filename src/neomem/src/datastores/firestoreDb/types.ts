//. firebase types - get from @firebase/firestore-types?
//. but This package is not intended for direct usage, and should only be used via the officially 
// supported firebase package.
export type Firebase = any;
export type Firestore = any;
export type Collection = any;
export type Document = any;
export type Path = string; // a firestore path, eg 'nodes/hda31u2'
